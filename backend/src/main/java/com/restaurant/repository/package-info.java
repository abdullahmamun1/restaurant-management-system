/**
 * Repository layer: Spring Data JPA repository interfaces that own all persistence
 * access. Services depend on these interfaces, never on the persistence provider directly.
 */
package com.restaurant.repository;
